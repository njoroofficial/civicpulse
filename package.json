{
  "name": "civicpulse",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "echo 'Start FastAPI: cd apps/api && uv run uvicorn main:app --reload'",
    "lint": "pnpm --filter web lint",
    "type-check": "pnpm --filter web type-check",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
